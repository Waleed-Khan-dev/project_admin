import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import PlaceFoam from '../Components/Places/PlaceFoam'

const AddPlace = () => {
  return (
    <PlaceFoam/>
  )
}

export default AddPlace

const styles = StyleSheet.create({})